# 前置就绪项核验报告

核验时间：2026-06-27

## 1. `.rpy` 编码规范化与 Ren'Py 语法预检查

状态：通过

- 已执行 `node work/tests/normalize_rpy.js`
- 扫描范围：`game/story/`、`game/gui/`
- `.rpy` 文件数：6
- 编码：UTF-8 无 BOM
- Ren'Py lint：退出码 0
- 李东升临终遗言存在：`老百姓以后会怎么样，只有天知道。`
- 统一收尾文案存在：`百姓心中有杆秤 时间会证明一切`

## 2. 本地 Git 仓库初始化与首次全量提交

状态：通过

- 本地仓库已初始化
- 当前分支：`main`
- 当前提交：`feb50a9 ci: skip pages deploy until web build exists`
- 本地 `main` 与远端 `origin/main` 当前提交一致

## 3. GitHub 授权、公开仓库与 origin 远端

状态：通过

- GitHub CLI 已登录账号：`orange051020`
- 远端仓库：`https://github.com/orange051020/chuimuo-visual-novel`
- 可见性：Public
- `origin` 已关联：
  - fetch：`https://github.com/orange051020/chuimuo-visual-novel.git`
  - push：`https://github.com/orange051020/chuimuo-visual-novel.git`

## 4. GitHub Pages 部署配置

状态：通过

- 公开访问地址：`https://orange051020.github.io/chuimuo-visual-novel/`
- HTTPS：已开启
- GitHub Pages 当前部署方式：GitHub Actions
- 工作流：`.github/workflows/pages.yml`
- 部署产物目录：`build-web`

说明：GitHub Pages 原生分支部署只支持 `/` 或 `/docs`，不支持 `/build-web`。因此本项目使用 GitHub Actions 上传 `build-web/`，实现等效的 `main` 分支源码 + `build-web` 目录产物部署。当前 `build-web/index.html` 尚未生成时，工作流会跳过部署而不失败。

## 5. 导入、校验、预构建脚本

状态：通过

- `chuimuo-visual-novel/tools/import_external_assets.js`：存在
- `work/tests/validate_assets_ready.js`：存在
- `chuimuo-visual-novel/tools/prebuild_check.js`：存在
- `chuimuo-visual-novel/tools/inject_web_meta.js`：存在
- `chuimuo-visual-novel/tools/write_acceptance_report.js`：存在
- `chuimuo-visual-novel/tools/validate_asset_manifest_consistency.js`：通过，60 个素材文件名完全匹配

## 6. Ren'Py SDK 与 renpyweb 编译环境

状态：通过

- Ren'Py SDK 路径：`C:\Users\12726\Documents\Codex\tools\renpy-sdk\renpy-8.5.3-sdk`
- Ren'Py 版本：`Ren'Py 8.5.3.26051504`
- `renpyweb`：`renpyweb shim for Ren'Py 8.5.3`
- 用户 PATH 已写入 SDK 路径
- 自动流水线脚本内会显式注入 SDK 路径，确保监听触发后可调用 `renpyweb`

## 自动触发规则

状态：通过

- 监听脚本：`work/auto_asset_pipeline.ps1`
- 当前监听进程：已运行
- 当前 `external-art-drop/` WebP 数量：0
- 触发条件：`external-art-drop/` 内 60 个 `.webp` 文件全部就位，且 `import_external_assets.js --dry-run` 文件名和格式预检查通过
- 美术禁令：Codex 不生成任何图片、占位图、代码绘图、矢量色块或纯色替代资源

## 当前唯一阻塞

状态：未通过

- 外部美术素材尚未投放
- `external-art-drop/` 当前缺失全部 60 张 `.webp`
- 导入、合规校验、renpyweb 编译、最终部署与全量验收将在素材投放后自动继续
